# Mirokaï images

Add robot photos here for the `/robots/mirokai` page:

- `mirokai-front.png` — front / face view (used as hero)
- `mirokai-side.png` — side profile showing ball base
- `mirokai-care.png` — nursing home, clinic, or hospitality context

Recommended: light background, full-body framing similar to `/images/neo-gamma/`.

Paths are referenced in `data/robots.ts`.
