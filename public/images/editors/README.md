# Editor avatars

Drop PNG files here (square, at least 128×128). Filenames must match editor IDs:

- `oguzhan-aydin.png`
- `chuck-steward.png`
- `maya-chen.png`

Paths are referenced in `data/editors.ts`. If a file is missing, the UI shows initials instead.
